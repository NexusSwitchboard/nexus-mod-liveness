{
  "name": "nexus-mod-liveness",
  "version": "0.0.1",
  "description": "Exposes a simple healthcheck endpoint in Nexus.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^2.0.0",
    "debug": "^4.1.1"
  },
  "devDependencies": {
    "@types/node": "^12.7.5",
    "@types/debug": "^4.1.5",
    "@types/express": "^4.17.1",
    "typescript": "^3.7.5",
    "tslint": "^6.0.0",
    "@nexus-switchboard/nexus-extend": "~0.4.2"
  },
  "peerDependencies": {
    "@nexus-switchboard/nexus-extend": "~0.4.2"
  }
}
